"use client";
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    common: {
      white: '#ffffff', // Add the white color here
    },
    // other palette properties...
  },
  // other theme properties...
});

export default theme;
